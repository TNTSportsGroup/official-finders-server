sudo: required
services:
  - docker
  - redis-server --port 6379

before_install:
  - docker build -t datdevboi/official-finders-server -f Dockerfile.dev .

script:
  - docker run datdevboi/official-finders-server npm test -- --coverage
