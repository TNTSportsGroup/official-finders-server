sudo: required
services:
  - docker
  - redis-server

before_install:
  - docker-compose -f docker-compose-dev.yml up -d --build
  #- docker exec -it official-finders-server-test bash
  #- docker build -t datdevboi/official-finders-server -f Dockerfile.dev .

script:
  #- docker exec -it official-finders-server-test bash
  - docker run official-finders-server_server npm run test -- --detectOpenHandles --collect-coverage
